name = "voca-tts-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Secret binding (set via: wrangler secret put ELEVENLABS_API_KEY)
# ELEVENLABS_API_KEY = <your_api_key>

# Optional: Restrict to specific routes
# routes = [
#   { pattern = "tts.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
