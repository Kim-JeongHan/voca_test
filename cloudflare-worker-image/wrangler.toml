name = "voca-image-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Secret binding (set via: wrangler secret put HUGGINGFACE_API_KEY)
# HUGGINGFACE_API_KEY = <your_api_key>

# Optional: Restrict to specific routes
# routes = [
#   { pattern = "image.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
